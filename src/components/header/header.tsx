import styles from './header.module.scss'

function Header()
{
    return(

        <section className={styles.main}>
            <img src="deve_logo.png"/>
            <img src="deve_logo.png"/>
        </section>

    )
}

export default Header;